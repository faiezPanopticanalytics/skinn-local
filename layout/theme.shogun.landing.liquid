{%- comment %}<locksmith:f977>{% endcomment -%}
  {%- include 'locksmith' -%}
{%- comment %}</locksmith:f977>{% endcomment -%}

{% comment %}
Auto-generated by Shogun.
This file can be re-written at any time.
{% endcomment %}

<!doctype html>
<html>
    
<head>
  
        <title>{{ page_title }}</title>
  {% include 'hic-tag' %}
  {% include 'hic-personalize' %}
        <meta charset="utf-8">
        <meta content="IE=edge,chrome=1" http-equiv="X-UA-Compatible">
        {%- comment %}<locksmith:7491>{% endcomment -%}
          {%- capture var %}{% render 'locksmith-variables', variable: 'transparent', hiding_scope: 'resource' %}{% endcapture %}{% if var == 'true' %}<meta name="description" content="{{ page_description | escape }}">{% endif -%}
          {%- comment %}original: <meta name="description" content="{{ page_description | escape }}">{%- endcomment %}
        {%- comment %}</locksmith:7491>{% endcomment -%}
        <link rel="canonical" href="{{ canonical_url }}">
        <meta content="width=device-width,initial-scale=1,shrink-to-fit=no" name="viewport">

        <!--SEO Imports-->
        {% if settings.enable_og  %}
            {% include "header_og_tags" %}
        {% endif %}

        {% if settings.enable_json_ld %}
            {% include "header_json_ld" %}
        {% endif %}

        <!-- Header hook for plugins -->
        {{ content_for_header }}

        <!--Font Files-->
        <script src="https://kit.fontawesome.com/2dc6b51dd5.js" crossorigin="anonymous"></script>
        <link href="https://fonts.googleapis.com/css2?family=Open+Sans:ital,wght@0,300;0,400;0,600;0,700;0,800;1,300;1,400;1,600;1,700;1,800&display=swap" rel="stylesheet">
        
        <!--CSS Files-->
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/3.5.2/animate.min.css">
        {{ 'theme.css' | asset_url | stylesheet_tag }}
        <style>
            [v-cloak] { display: none; }
        </style>
        <!--Above the fold script tags-->
        {{ 'vue.js' | asset_url | script_tag }}
        
        <!--Header Plugin Imports-->
        {% include 'swymSnippet' %}
        
        {% if settings.enable_yotpo_reviews %}
            {% include 'yotpo-install' %}
        {% endif %}
    
  <script src="https://cdn-widgetsrepository.yotpo.com/v1/loader/2Wb1IkGjRPj67cIA_uZF5g" async></script>
</head>


    
<body>
  
  
  {{ content_for_layout }}
  {{ 'theme.js' | asset_url | script_tag }}
  <script src="https://js.afterpay.com/afterpay-1.x.js" async></script>
<script src="http://cdn.jsdelivr.net/g/lazysizes(lazysizes.min.js+plugins/rias/ls.rias.min.js+plugins/optimumx/ls.optimumx.min.js)" async=""></script>
  <style type="text/css">
    body, html {
      width: 100% !important;
      background: white !important;
    }
  </style>
  
</body>

</html>

